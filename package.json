{
    "name": "podcast-api",
    "version": "0.0.1",
    "scripts": {
        "deploy": "tsc && wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "types": "wrangler types ./types/wrangler.d.ts"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.8.66",
        "typescript": "^5.9.2",
        "vitest": "~3.2.4",
        "wrangler": "^4.32.0"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^0.0.86"
    }
}
