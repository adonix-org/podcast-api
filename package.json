{
    "name": "podcast-api",
    "version": "0.0.1",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "cf-typegen": "wrangler types"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.8.63",
        "typescript": "^5.9.2",
        "vitest": "~3.2.4",
        "wrangler": "^4.30.0"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^0.0.30"
    }
}
