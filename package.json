{
    "name": "podcast-api",
    "version": "0.0.1",
    "scripts": {
        "deploy": "tsc && wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "types": "wrangler types ./types/wrangler.d.ts"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.9.0",
        "typescript": "^5.9.2",
        "vitest": "~3.2.4",
        "wrangler": "^4.42.0"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^0.0.172"
    }
}
