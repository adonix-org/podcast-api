{
    "name": "podcast-api",
    "version": "0.0.1",
    "scripts": {
        "deploy": "tsc && wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "vitest",
        "types": "wrangler types ./types/wrangler.d.ts"
    },
    "devDependencies": {
        "typescript": "^5.9.3",
        "wrangler": "^4.51.0"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^1.0.0"
    }
}
